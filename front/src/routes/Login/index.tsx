import { FC } from 'react';
import { Box } from '@mui/material';
const Login: FC = () => {
  return (
    <Box
      sx={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>Login</h1>
    </Box>
  );
};

export default Login;
